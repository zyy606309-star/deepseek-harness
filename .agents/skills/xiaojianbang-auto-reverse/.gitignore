# Prebuilt native binaries / JAR / kernel modules shipped with the skill.
# They stay on disk (the local skill needs them to run) but are excluded from
# git: they cannot be diffed and would bloat repository history. Recover them
# from the original skill zip (【26.07.03】xiaojianbang-auto-reverse.zip) if a
# fresh clone needs the full toolchain.
scripts/tools/ecapture-v2.3.0-android-arm64/ecapture
scripts/tools/dexfixer/dexfixer.jar
scripts/tools/MemDumper/libs/
scripts/tools/xiaojianbang-stealth-hook/release/
scripts/tools/xiaojianbang-stealth-hook/userspace/
scripts/tools/xiaojianbang-syscall-filter/syscallhook.kpm
scripts/tools/kernelpatch-kpatch/kpatch
